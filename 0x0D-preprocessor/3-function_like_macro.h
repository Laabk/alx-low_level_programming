#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACOR_H

/**
 * main - Using a function-like macro ABS(x) that computes the absolute value of a number x.
 */


#define ABS(x) ((x) < (0) ? -(x) : (x))


#endif
